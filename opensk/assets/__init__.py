"""Readers for True Skate's own asset bundle.

Nothing here ships game data. These decode files from a bundle the USER
already owns, on their machine, so Open Skate's geometry can be checked
against the real thing instead of against a guess. Decoded art is written to
a gitignored directory and never committed: this repo is public.
"""
